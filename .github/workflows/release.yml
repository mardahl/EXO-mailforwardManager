name: Release

on:
  push:
    tags: ['v*.*.*']

permissions:
  contents: write

jobs:
  package:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Build release zip
        run: |
          stage="EXO-mailforwardManager-${GITHUB_REF_NAME}"
          mkdir "$stage"
          cp MailboxForwardingTool.ps1 Launch-MailboxForwardingTool.bat config.example.json README.md LICENSE CHANGELOG.md "$stage/"
          zip -r "${stage}.zip" "$stage"

      - name: Attach to release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          ver="${GITHUB_REF_NAME#v}"
          awk -v ver="$ver" '
            $0 ~ "^## \\[" ver "\\]" { flag=1; next }
            flag && /^## \[/ { exit }
            flag { print }
          ' CHANGELOG.md > notes.md
          gh release upload "$GITHUB_REF_NAME" EXO-mailforwardManager-*.zip --clobber \
            || gh release create "$GITHUB_REF_NAME" EXO-mailforwardManager-*.zip --notes-file notes.md
